services:
  - type: cron
    name: lifeup-scheduler
    env: python
    plan: starter
    buildCommand: "pip install -r requirements.txt"
    
    # 🟦 Chạy mỗi 10 phút
    cronSchedule: "*/10 * * * *"

    startCommand: "python scheduler.py"
    environment:
      pythonVersion: 3.11.6
